import {$authHost} from "./index";



export  const addDeviceBasket = async (basket) => {
    // const {data} = await $authHost.post('api/basket', basket)
    // return data
}

export  const fetchBasketDevice = async (userId) => {
    // const {data} = await $authHost.get(`/api/basket/?userId=${userId}`)
    // return data
}

export  const deleteBasketDevice = async (basket) => {
    // const {data} = await $authHost.delete('/api/basket/', {params: basket})
    // return data
}